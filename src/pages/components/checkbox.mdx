---
layout: "../../layouts/Docs.astro"
title: "Checkbox"
description: "Checkbox component"
slug: "checkbox"
toc: true
soon: false
---
import Example from '../../components/Example.astro'
import Checkbox from '../../components/ui/Checkbox.astro'

<Example>
  <Checkbox />
</Example>

```astro
<Checkbox />
```

## Sizes

Los checkbox se pueden mostrar en diferentes tamaños. Por defecto, el tamaño es `md`. Los tamaños disponibles son `sm`, `md`, `lg` y `xl`. Para cambiar el tamaño, se debe usar la propiedad `size`.

<Example>
  <div class="flex items-center gap-6">
    <Checkbox size="sm" />
    <Checkbox size="md" />
    <Checkbox size="lg" />
    <Checkbox size="xl" />
  </div>
</Example>

## With label

<Example>
  <label class='flex items-center gap-1 cursor-pointer'>
    <Checkbox />
    <span class="ml-2">Label</span>
  </label>
</Example>

## Light

Los checkbox se pueden mostrar en modo `light`. Para cambiar el modo, se debe usar la propiedad `light`.

<Example isDark>
  <Checkbox light />
</Example>

```astro
<Checkbox light />
```

## Checked

Los checkbox se pueden mostrar como `checked`. Para cambiar el modo, se debe usar la propiedad `checked`.

<Example>
  <Checkbox checked />
</Example>

```astro
<Checkbox checked />
```

## Disabled

Los checkbox se pueden mostrar como `disabled`. Para cambiar el modo, se debe usar la propiedad `disabled`.

<Example>
  <Checkbox disabled />
</Example>
  
```astro
<Checkbox disabled />
```