---
layout: "../../layouts/Docs.astro"
title: "Drawer"
description: "Los drawers son elementos de la interfaz de usuario que permiten a los usuarios seleccionar una opción de una lista de opciones. Los drawers se utilizan para agrupar opciones relacionadas y mostrarlas en una lista desplegable."
slug: "drawer"
toc: true
soon: false
image: "drawer"
---
import Example from '../../components/Example.astro'
import Button from '../../components/ui/Button.astro'
import Drawer from '../../components/ui/Drawer.astro'

## Usage

<Example>
  <Button data-toggle='drawer-example'>Drawer</Button>
  <Drawer id='drawer-example'>
    <h2 slot='title' class='font-medium text-xl'>Title here</h2>
    <div slot='body'>
      <p>Drawer body text goes here.</p>
    </div>
    <div slot='footer' class='flex items-center justify-end gap-2'>
      <Button type='button' data-dismiss='drawer' variant='secondary'>Close</Button>
      <Button type='button' variant='primary'>Save changes</Button>
    </div>
  </Drawer>
</Example>

```astro
<Button data-toggle='drawer-example'>Drawer</Button>
  <Drawer id='drawer-example'>
    <h2 slot='title' class='font-medium text-xl'>Title here</h2>
    <div slot='body'>
      <p>Drawer body text goes here.</p>
    </div>
    <div slot='footer' class='flex items-center justify-end gap-2'>
      <Button type='button' data-dismiss='drawer' variant='secondary'>Close</Button>
      <Button type='button' variant='primary'>Save changes</Button>
    </div>
  </Drawer>
```

## Placement

Para cambiar la posición del drawer, use el atributo `placement` con los valores `left` o `right`. Por defecto, el drawer se muestra en la parte izquierda de la pantalla.

<Example>
  <div class='flex items-center justify-start gap-4'>
    <Button data-toggle='drawer-left'>Drawer left</Button>
    <Drawer id='drawer-left' placement='left'>
      <h2 slot='title' class='font-medium text-xl'>Title here</h2>
      <div slot='body'>
        <p>Drawer body text goes here.</p>
      </div>
      <div slot='footer' class='flex items-center justify-end gap-2'>
        <Button type='button' data-dismiss='drawer' variant='secondary'>Close</Button>
        <Button type='button' variant='primary'>Save changes</Button>
      </div>
    </Drawer>
    <Button data-toggle='drawer-right'>Drawer right</Button>
    <Drawer id='drawer-right' placement='right'>
      <h2 slot='title' class='font-medium text-xl'>Title here</h2>
      <div slot='body'>
        <p>Drawer body text goes here.</p>
      </div>
      <div slot='footer' class='flex items-center justify-end gap-2'>
        <Button type='button' data-dismiss='drawer' variant='secondary'>Close</Button>
        <Button type='button' variant='primary'>Save changes</Button>
      </div>
    </Drawer>
  </div>
</Example>

```astro
<Button data-toggle='drawer-left'>Drawer left</Button>
<Drawer id="drawer-left" placement='left'>
  ...
</Drawer>

<Button data-toggle='drawer-right'>Drawer right</Button>
<Drawer id="drawer-right" placement='right'>
  ...
</Drawer>
```

## Sizes

Los tamaños disponibles para los drawers son `sm`, `md`, `lg`, `xl`, `2xl`, `3xl`, `4xl`, `5xl`, `6xl`, `7xl`, y `full`. Por defecto, el tamaño es `xs`. Para cambiar el tamaño, use el atributo `size` con el valor deseado.


