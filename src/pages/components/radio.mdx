---
layout: "../../layouts/Docs.astro"
title: "Radio"
description: "Radio component"
slug: "radio"
toc: true
soon: false
---
import Example from '../../components/Example.astro'
import Radio from '../../components/ui/Radio.astro'

<Example>
  <Radio />
</Example>

```astro
<Radio />
```

## Sizes

Los radio se pueden mostrar en diferentes tamaños. Por defecto, el tamaño es `md`. Los tamaños disponibles son `sm`, `md`, `lg` y `xl`. Para cambiar el tamaño, se debe usar la propiedad `size`.

<Example>
  <div class="flex items-center gap-6">
    <Radio size="sm" />
    <Radio size="md" />
    <Radio size="lg" />
    <Radio size="xl" />
  </div>
</Example>

## With label

<Example>
  <label class='flex items-center gap-1 cursor-pointer'>
    <Radio />
    <span class="ml-2">Label</span>
  </label>
</Example>

## Light

Los radio se pueden mostrar en modo `light`. Para cambiar el modo, se debe usar la propiedad `light`.

<Example isDark>
  <Radio light />
</Example>

```astro
<Radio light />
```

## Checked

Los radio se pueden mostrar como `checked`. Para cambiar el modo, se debe usar la propiedad `checked`.

<Example>
  <Radio checked />
</Example>

```astro
<Radio checked />
```

## Disabled

Los radios se pueden mostrar como `disabled`. Para cambiar el modo, se debe usar la propiedad `disabled`.

<Example>
  <Radio disabled />
</Example>
  
```astro
<Radio disabled />
```

